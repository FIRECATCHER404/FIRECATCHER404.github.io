{
  "rules": {
    ".read": false,
    ".write": false,
    "posts": {
      "$postId": {
        ".read": "data.child('published').val() === true || (auth != null && auth.token.admin === true)",
        ".write": false
      }
    }
  }
}